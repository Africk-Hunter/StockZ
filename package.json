{
  "name": "websiteproject",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently -n css,netlify -c yellow,cyan \"npm:css:watch\" \"netlify dev\"",
    "css:watch": "tailwindcss -i ./public/src/input.css -o ./public/src/output.css --watch",
    "build": "tailwindcss -i ./public/src/input.css -o ./public/src/output.css --minify"
  },
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.19",
    "concurrently": "^10.0.5",
    "postcss": "^8.4.38",
    "tailwindcss": "^3.4.3"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "firebase": "^10.12.0",
    "undici": "^8.10.0"
  }
}
